amp-analytics(type="googleanalytics")
  script(type="application/json").
    {
      "vars": {
        "account": "UA-96600709-2"
      },
      "triggers": {
        "trackPageview": {
          "on": "visible",
          "request": "pageview"
        }
      }
    }
